#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int x;
        string s;
        cin >> s;
        vector<int> v;
        for (int i = 0; i < s.size(); i++)
        {

            if (s[i] != '0')
            {
                x = 
            }
        }
    }
    return 0;
}